@{
    string fontZoom = this.ViewData.ContainsKey("FontZoom") ? this.ViewData["FontZoom"].ToString() : "100";
}
<style type="text/css">
    @@media print {
        #printReceiptDiv {
            height: auto;
            font-size: @fontZoom;
            page-break-inside: avoid;
        }
    }
</style>
